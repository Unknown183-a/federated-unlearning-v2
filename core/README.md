# core/

Python ML logic for the Federated Unlearning Lab (dataset loading, FL,
unlearning engine, evaluation). Mirrors the structure in
`docs/FEDERATED_UNLEARNING_DASHBOARD_SPEC_V2.md`, Chapter 49.

This is currently a skeleton — every file is a placeholder populated in
the phase noted in its docstring. No real ML code belongs here until
that phase starts.
